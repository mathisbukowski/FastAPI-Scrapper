# FastAPI Scrapper Environment Variables

# Application Settings
APP_NAME=FastAPI Scrapper
APP_VERSION=1.0.0

# Server Settings
HOST=0.0.0.0
PORT=8000

# Database Settings (do NOT commit real credentials in .env, only here in example)
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fastapi_scrapper

# A complete URL can be constructed by the app, avoid hardcoding:
# postgresql://$DB_USER:$DB_PASSWORD@$DB_HOST/$DB_NAME
# Or you can export DATABASE_URL externally instead of these four.
